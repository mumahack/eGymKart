import {expect} from 'chai';
describe('egym-adapter', () => {
    let myStub;
    beforeEach(() => {
        myStub =  {};
    });

    it('test works', () => {
        expect(true).to.be.true;
        expect('test').to.equal('test');
    });
});