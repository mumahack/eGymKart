import {expect} from 'chai';
describe('Calculator', () => {
  let myStub;
  beforeEach(() => {
     myStub =  {};
  });

  it('does not divide by 0', () => {
    expect(true).to.be.true;
    expect('test').to.equal('test');
  });
});